// =====================================================================
// !! DO NOT TRUST THIS DOCUMENT. ASK THE OWNER.
//
// Nothing in docs/ is authoritative -- not the handoffs, not the specs,
// not the ones the owner asked for himself. This file describes what was
// true when somebody wrote it, and this project has repeatedly proven
// that "true when written" and "true now" are different things.
//
// Verify anything you are about to act on against the DISK, the
// COMPILER, or the RUNNING GAME. Ask the owner about anything to do with
// scope, priority, or what to build next. Never inherit a task from a
// document.
//
// Real examples from this repo, every one a document confidently stating
// something false: an engine path that did not exist for weeks; a source
// pack path pointing at the wrong pack; "87% of sounds are missing" that
// was true when written and false when read; a rule forbidding an
// extraction that had already happened; a note that ORDERED sessions not
// to attempt a fix that turned out to be four lines.
//
// Banner added 2026-08-07 at the owner's instruction.
// =====================================================================

================================================================================
   RS_24 -- THE CH PARENT PROPERTIES.  2026-08-05.
   The half of the monster port that was missing all four times.
   Read after rs_21 (THE PORT LAW) and rs_23.
================================================================================

WHAT WAS WRONG
--------------------------------------------------------------------------------

  The owner's report: "they all seem to fire once while walking around,
  sometimes they fire a custom sprite, but the behavior doesn't seem too
  different... the behavior itself seems like just, zombieman."

  He was right, and every state block was correct. All fourteen Zombieman
  tiers were diffed against CHP line by line on 2026-08-05 and they MATCH --
  T01's double-shot and permanent gas walk, T06's fastchase interleave,
  T07's no-op Missile plus kamikaze Melee, T08's `See+3` re-entry, T10's
  `Missile2+1` loop that skips the wind-up. The port of the states is good.

  THE STATES ARE ONLY HALF OF A CHP ACTOR.

      ACTOR CommonRedZombie : RedZombie

  CHP files carry the STATES. Every combat PROPERTY -- flags, species,
  radius/height/mass/scale, damage factors, blood colour -- lives on the CH
  PARENT in CH/decorate/<Family>.txt. Four porting attempts read CHP and
  never opened the parent. Result: fourteen tiers running one identical
  property set, with only hp/speed/painChance varying.

  THE TWO THAT COST THE GRAMMAR:

    +MISSILEMORE  Halves the "don't fire" distance roll (modern spelling:
                  MissileChanceMult 0.5 -- LOWER FIRES MORE). CH puts it on
                  10 of 14 zombie tiers. Missing from all of them, so every
                  tier fired on the timid vanilla schedule. CH's design is a
                  CONTRAST -- T00/T06/T08/T10 cautious, the rest aggressive.
                  We had flattened all fourteen to cautious.

    +AVOIDMELEE   Keeps the monster at range instead of closing. CH puts it
                  on 9 of 14. Missing, so they walked into your face -- and
                  once there, the second defect below trapped them.

  AND A SEPARATE, COMPOUNDING BASE-CLASS DEFECT:

    RS_MonsterMaster's dispatcher declares `Melee:` for EVERY monster, so
    MeleeState was non-null on every tier of every family -- including the
    ones with no melee attack. Two consequences:
      1. P_CheckMissileRange does `if (MeleeState == NULL) dist -= 128;`
         -- "no melee attack, so fire more". Every tier lost that bonus.
      2. A_Chase checks melee FIRST and returns. At melee range a
         melee-less tier went Melee: -> TierState null -> Goto See ->
         chase -> Melee: ... and never reached its missile state.
    Of the 15 family-01 CHP actors only 01_F (T07) and 01_K (T11) define
    Melee:. The other thirteen have none, and get the -128.

  NONE of this is visible to any lint. It is the exact disease CLAUDE.md
  names: the tree was consistent with itself and wrong.

WHAT WAS BUILT
--------------------------------------------------------------------------------

  RS_MonsterMaster.zs
    * ApplyTier now re-points the state pointers per tier:
          MeleeState   = TierState("Melee");
          MissileState = TierState("Missile");
      TierState returns null when neither Melee.<tier> nor Melee.T00
      exists -- exactly the melee-less set. Family-agnostic.
    * RS_MonsterTierRow gained the CH property fields: flags, species,
      radius, height, mass, scale, gibHealth, bloodColor, missileChance.
    * RS_TF_* flag consts at file scope, named for what CH writes so the
      table diffs against CH/decorate without translation.
    * RS_ApplyTierProperties(row) applies them. FLAGS ARE ASSIGNED, NEVER
      OR'd -- the row is an absolute statement of the tier, so a flag T03
      sets is CLEARED when the monster becomes T04.
    * virtual TierDamageFactor(t, damageType) + a DamageMobj override.
      Per-type DamageFactors live on class DEFAULTS in ZScript and cannot
      be assigned per instance; this is the per-instance equivalent.

  RS_Zombieman.zs
    * All fourteen tiers' CH properties transcribed with parent name and
      Zombies.txt line number on every case.
    * TierDamageFactor override: DIMp 0 family-wide, Exorcist 3.0 on
      T00-T10, Heroic 3.0 on the three bosses, Fire/Melee 2.0 on cyan and
      gray, fire 0.25 on the fire zombie, Melee 3 on red, PlayerVoid 0.5
      and Falling 0.0 on TEX.
    * Mass and +ROLLSPRITE REMOVED from OnTierApplied. They were already
      correct there -- but OnTierApplied runs AFTER RS_ApplyTierProperties,
      so leaving them would have silently overwritten the row. One owner.

  DELIBERATELY RECORDED, NOT APPLIED:
    * bloodColor. It is a `color` field and setting it at runtime does
      nothing -- the engine bakes BloodTranslation at class init. The CH
      values are in the row because they are ground truth and finding them
      again costs a read of CH/decorate. Applying them needs a
      BloodTranslation rebuild, which is its own job.
    * +FLOORCLIP is NOT an assigned tier flag. Every CH parent has it and
      so does every Default here; making it absolute would silently clear
      it for the sixteen families with no table yet.

  WHAT T11/T12/TEX ACTUALLY ARE -- recorded because it surprised the owner
  and rs_23 did not say it plainly. Family 01 stops being zombiemen at the
  top. T11 is Player 9 (ZOMK, a dead marine), T12 is the UNDERTAKER (MAGE,
  a robed necromancer with a shovel), TEX is Player X (ZMKX). Only those
  THREE carry +BOSS -- T10 Red is not a boss, it is a nasty zombie that
  always gibs. All three bosses share one profile: +BOSS +QUICKTORETALIATE
  +LOOKALLAROUND +MISSILEMORE +NOFEAR +DONTMORPH -NORADIUSDMG. The
  -NORADIUSDMG is deliberate: CH wants them to take splash.

  T12's `Species "UnderTaker"` IS SHARED WITH MrBones. That is how the
  summoner does not shred its own skeletons. Drop it and the Undertaker
  kills its whole economy.

THE PLAN FOR FAMILIES 02-17
--------------------------------------------------------------------------------

  THIS IS A READ AGAINST CH/decorate, NOT A REWRITE. For each family:
  open the CH file, extract the property block of each tier's parent, fill
  in a TierData property switch exactly as Zombieman's now reads. The
  state blocks are not touched.

  Measured scope -- actor counts include spawn-colour variants, so the
  number of PARENTS to read is roughly one per tier, not per actor:

    family              CH file             actors  +MISSILEMORE  +AVOIDMELEE
    01 Zombieman        Zombies.txt             58        10           9   DONE
    02 Shotgunner       Shotgunners.txt         60        14           4
    03 Imp              Imps.txt                65        20           0
    04 Chaingunner      Chaingunners.txt        64        18           7
    05 Demon            Demons.txt              48         5           0
    06 Spectre          spectres.txt            35         1           0
    07 LostSoul         lostsouls.txt           53        26           0
    08 Cacodemon        Cacodemons.txt          81        16           0
    09 PainElemental    thepains.txt            83        11           2
    10 HellKnight       Hellknights.txt         74        14           0
    11 Baron            Barons.txt              97        20           0
    12 Revenant         Revenants.txt          112        25           0
    13 Mancubus         Fatsos.txt              87         7           0
    14 Arachnotron      Spiders.txt            110        24           0
    15 Archvile         Archviles.txt          106        11           1
    16 Mastermind       MASTERMINDS.txt        108        17           1
    17 Cyberdemon       CYBIES.txt             140        15           0

  READ THE ZERO COLUMNS AS DATA, NOT AS ABSENCE. +AVOIDMELEE is a ranged
  attacker's flag -- the four families that have it are the four human
  gunner families plus the Pain Elemental, Archvile and Mastermind. A
  Baron does not avoid melee because a Baron IS melee. The distribution
  is the design; do not normalise it.

  +MISSILEMORE is on every family and is the single highest-value item on
  this list. 246 instances across the seventeen files, none of them ported.

  ORDER: rs_23's depth-first order still holds -- 02 Shotgunner, then 04
  Chaingunner, then 03 Imp. Shotgunner and Chaingunner share the human
  gunner shape with Zombieman, so their tables read almost identically and
  the diff is cheapest.

  DO NOT BULK-SWEEP THIS. The Zombieman table took a read of every parent
  with the line number written down next to each case. A mechanical pass
  that copies flags without opening both sides is the exact failure mode
  that produced four bad ports.

STILL OWED -- NEEDS ONE COMPILE
--------------------------------------------------------------------------------

  Nothing in this batch has been through a build. The lines most likely to
  be rejected, in order:

    1. MeleeState / MissileState assignment in ApplyTier. If ZScript
       treats them as readonly on this engine, the -128 half cannot be
       fixed this way and the fallback (MeleeRange = -1000 on melee-less
       tiers) only kills the melee bounce, not the aggression loss.
    2. The DamageMobj override signature in RS_MonsterMaster.
    3. Species = r.species  (string -> Name conversion).
    4. bLAXTELEFRAGDMG, bQUICKTORETALIATE, bNOTARGETSWITCH spellings.
    5. RS_NoHitscan's AMRK sprite anchor -- the mod ships no sprite for
       it, so it resolves from the IWAD/gzdoom.pk3 or not at all. If it
       errors, any existing 1-frame sprite works; the model is what draws.

================================================================================


================================================================================
   APPENDIX A -- THE FIVE-AGENT CH SWEEP.  2026-08-05.  THE WORKING SET.
================================================================================

THE SIX RULES THIS SWEEP ESTABLISHED

 1. CHP INVERTS CH FLAGS, IT DOES NOT ONLY ADD TO THEM. A union pass is
    wrong. Removal sites: -MISSILEEVENMORE on Revenant T03/T07/T08
    (08_CY:10, 08_F:9, 08_BR:10) but NOT T04; Imp TEX (03_KX:8); Baron
    T09/T12 (15_GY:6, 15_W:3). -MISSILEMORE on Caco T11/TEX (09_K:10,
    09_KX:11), PainEl T03 (10_CY:9), Mastermind T06 (16_A:8), Arachno
    T03 (12_CY:10). -FLOATBOB Caco T03, PainEl T07. -NOPAIN LostSoul
    T07, Baron T07, Mastermind T10, Cybie T06. -FLOORCLIP Mancubus T06.
    -BOSS PainEl T09. -FULLVOLACTIVE Cybie T08.
    AND THE OTHER WAY: +MISSILEEVENMORE added to Mancubus T12 (13_W:9)
    and Caco T08 (09_BR:9); +FLOAT/+NOGRAVITY/+FLOATBOB re-enabled on
    Arachnotron T03/T05/T07 and Mastermind T10 where CH sets all three
    MINUS. The row being an ABSOLUTE statement is what survives this.

 2. "NONE STATED" MAPS ONTO WHETHER THE TIER HAS A VANILLA PARENT.
    Tiers declared : HellKnight / : PainElemental / : Doomimp state
    almost nothing and lean on the engine; bare-Actor tiers state
    everything. Inventing a default is wrong in OPPOSITE directions on
    the two kinds.

 3. FOUR TIERS HAVE NO CH PARENT AT ALL, whole block is CHP's:
    Chaingunner T09 (04/04_GY), Demon T09 (06/06_GY), Mastermind T12
    (16_W), Arachnotron T14 (12_WX), plus every Archvile/Cyberdemon EX
    rung. A "read the CH parent per tier" loop returns EMPTY here.

 4. ORPHAN DUPLICATES AT THE CHP DECORATE ROOT disagree with the folder
    copies: 04GY.txt, 06GY.txt, 14WX.txt, 16W.txt. DECORATE.txt includes
    the FOLDER copies (lines 704, 735). Root copies are dead.

 5. PROPERTIES SET TWICE IN ONE BLOCK -- DECORATE TAKES THE LAST:
    MeleeRange 102 then 88 (Revenant T03, T08); Mass 1500 then 2000
    (Mancubus T05/T07/T10); Mass 3000 then 1000 (CyanCybie2 CY:793/812);
    xScale 1.0 then 1.15 (BrownBaron2); ice 0.15 then 0.50 (BrownMind2
    MM:86/88); Fire 0.75 then 0.5 (YellowHK); fire 8 then 4
    (YellowDemon); Fire 0.7 then 1.50 (CyanBaron2 -- the ICE baron
    effectively BURNS).

 6. RUNTIME FLAG CHANGES LIVE IN THE STATE LAYER, NOT THE ROW. Red
    Revenant, Red Fatso, WhiteSpider3, Archvile T10/T12, Mastermind
    T05/T10/T11, Cybie T05/T06/T09/T10/T11 all turn MISSILEEVENMORE on
    mid-fight with A_ChangeFlag. Do NOT put those in TierData.

+AVOIDMELEE -- WHERE IT ACTUALLY IS
  ZERO hits in Imps, Demons, spectres, lostsouls, Cacodemons, Revenants,
  Fatsos, Spiders, Hellknights, Barons, CYBIES. It is a ranged
  attacker's flag: Zombieman 9/14, Shotgunner 4, Chaingunner 7,
  PainElemental T12 only, Archvile T12+T14 only, Mastermind T12 only.
  Do not "even it out".

SPECIES MAP -- THE INFIGHTING WEB
  Imp        "Imp" all 14 -- UNIFORM.
  Baron      "BaronOfHell" all 13, SHARED with Hell Knights (except HK
             T12) and its own tentacles -- UNIFORM.
  Chaingunner SIX: "CGuy"(T00) "Cguy"(T02, case-only) "Cguy2"(T04)
             "Cguy3"(T05,T07) "BrownCguy"(T08) "Science"(T12); 7 none.
  Shotgunner "SGuy"(T00,T02) "SGuy2"(T04,T08) "SGuy3"(T01)
             "BlackSG"(T11) "BENE"(TEX); T09 none.
  Mastermind "MMind2"(T00) "MMind"(T01,02,07,08,09,12) "MMind3"(T04);
             T03,05,06,10,11 none. SpecialSpider1 -- the BLUE mind's
             summon -- carries "MMind3", the PURPLE one's. CH bug;
             reproduce verbatim.
  Cyberdemon "Cybie" T00-T09+TEX; "Daikatana" T12,T14; T10,T11 none.
             SpecialHK, the blue cybie's escort Hell Knight, is given
             "Cybie" -- folded into the cyberdemon bloc.
  Archvile   "vile1" most; NONE on T03/T12/RedArch2; "MasterofTime" T14.
             THE RESURRECTOR'S SUMMONS SHARE IT: SpecialRev (AV:3008)
             and SpecialVile (AV:3445) are both Species "Vile1" -- the
             Undertaker/MrBones pattern again.
  Demon      "Demon1" most; "Demon"(T06); "Butcher"(T11).
  Spectre    "Demon1" most; "Demon"(T06 -- ONE CHARACTER drops that tier
             out of the bubble); none on T11/T12/TEX.
  LostSoul   "LSoul" T00-T10; "HORNET"(T11 Queen Bee, swarm is damagetype
             "hornet" with damagefactor "hornet",0); "whitelsoul"(T12,TEX).
  Mancubus   "Fatso"(7) "Fatso2"(T05,T07,T10); T06,11,12,TEX none.
  PainElem   "PE" all but T11. T12 SHARES "PE" with MiniSentinelPE, its
             escort -- the only PE tier that species-protects its summon.
             T11 Overlord instead uses DamageFactor "hornet",0.
  HellKnight "BaronOfHell" except T12 "WhiteHK"; T03/T08 none.
  Revenant   "revenant" most; "MontyP"(T11,TEX); T12 Lich none AND
             +NOINFIGHTING.
  Arachnotron "Spider1" most; "WhiteSP"(T12,T14,T15); T04 none AND no
             +DONTHARMSPECIES -- the designated infighter on two axes.

TIERS CHP SHIPS THAT WE HAVE NO CLASS FOR
  15_R2 RedBaron1      2000 HP GROUND bruiser, NOT the Fallen -- keeps
                       "Blessed", no +Float. CHP ships two red-Baron
                       tier files; we have one.
  14_R2 RedArch2       CH's RedArch is an ACS coin-flip between RedArch3
                       and RedArch2. The red vile slot is TWO monsters.
  17_WX WhiteCybieEX2  1.85 MB, Health 32767, "Daikatana". A full T14
                       rung; our header stops at TEX.
  05_K2 BlackLSoulOld  second, weaker Queen Bee (666 vs 1500 HP).
  12_W2 WhiteSpiderOld
  Phase forms with their OWN property blocks, spawned mid-fight and not
  spawnable tiers: CommonBlackArchEX3, CommonWhiteArchEX3,
  CommonWhiteCybieEX3.

BUG IN OUR EXISTING CODE -- NOT A MISSING TABLE
  RS_BaronFallen (RS_Baron.zs:1493) against CH RedBaron2:
    Height 56, CH says 48   (Barons.txt:3606)
    Mass  400, CH says 200  (Barons.txt:3608)
    MISSING DamageFactor Melee,5.0 (Barons.txt:3616-3617). The Fallen is
    meant to take FIVE TIMES melee damage. That is its entire
    counterplay and we do not have it.

THREE DAMAGE ROLLS ON TIER PARENTS -- DO NOT FLATTEN
  BlackDemon3    Damage (random(10,40))  Demons.txt:1816
  WhiteDemon2    Damage (random(20,60))  Demons.txt:2140
  FireBluCybie2  Damage (random(15,70))  CYBIES.txt:1974
  All three go through DamageFunction per CLAUDE.md.

SELF-IMMUNITY DAMAGE TYPES -- LOAD-BEARING
  "DIMp",0 family-wide; "Moloch",0 (RedCybie CY:3319);
  "Supremesmith",0 (17_KX:12); "stophurtinghimalready",0 (17_WX:16);
  "hornet",0 (BlackPE2 thepains:1988). These are how a boss survives its
  own attack. Drop one and the boss kills itself.

GIBHEALTH
  Stated NOWHERE in Archviles/MASTERMINDS/CYBIES. FIVE MANCUBUS PARENTS
  HAVE A POSITIVE GibHealth 70, and GreenDemon has +45; CHP flips them
  negative. Porting CH's value makes those monsters explode on any death.
